import { Login, Request, SendTransaction } from './'

describe('Login', () => {
  it('is truthy', () => {
    expect(Login).toBeTruthy()
  })
})

describe('Request', () => {
  it('is truthy', () => {
    expect(Request).toBeTruthy()
  })
})

describe('SendTransaction', () => {
  it('is truthy', () => {
    expect(SendTransaction).toBeTruthy()
  })
})
